[project]
name = "dooit-extras"
version = "0.1.0"
description = "Bar utils for Dooit"
readme = "README.md"
requires-python = ">=3.8.1"
dependencies = [
    "dooit",
]

[tool.setuptools]
packages = { find = { exclude = ["node_modules"] } }

[tool.uv.sources]
dooit = { git = "https://github.com/kraanzu/dooit", branch = "develop" }
